highligher: pygments
permalink: "/:year/:month/:day/:title.html"
paginate: 20
paginate_path: "/posts/:num/"
gems:
  - jekyll-redirect-from
sass:
  sass_dir: styles
defaults:
  -
    scope:
      path: ""
      type: "posts"
    values:
      layout: "posts"
